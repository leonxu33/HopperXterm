// Package profile persists the user's session catalog: groups and sessions
// (flat model — no nested folders, no separate Server entity). Stored as
// JSON under os.UserConfigDir()/hopperxterm/.
package profile
